const Button = (props) => {
    return ( <button className="bg-primaryColor h-[50px] rounded-full opacity-100 uppercase font-semibold text-center px-[28px] py-[15px]">{props.text}</button> );
}
 
export default Button;