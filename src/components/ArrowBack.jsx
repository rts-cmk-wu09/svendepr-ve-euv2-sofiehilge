import { IoIosArrowRoundBack } from "react-icons/io";


const ArrowBack = (props) => {
    const {color} = props;
    return ( <IoIosArrowRoundBack color={color} size={28}/> );
}
 
export default ArrowBack;